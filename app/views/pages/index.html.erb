<p id="notice"><%= notice %></p>

<h1>Menu label links</h1>

<table>
  <thead>
    <tr>
      <th>Menu label links:</th>
    </tr>
  </thead>

  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= link_to page.menu_label, page %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Page', new_page_path %>
